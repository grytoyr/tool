<% if params[:action] == "success" %>

<% elsif params[:action] == "home" %>

  <nav class="navbar navbar-expand-lg navbar-light navbar-tool">

    <div class="left-menu-wrap">


      <!-- Logo -->
      <%= link_to root_path, class: "navbar-brand" do %>
        <%= image_tag "logo.svg", class:"logo" %>
        <% end %>

  </div>

    <div class="right-menu-wrap">

      <!-- Categories -->
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Categories
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <% Item::CATEGORIES.each do |item| %>
                <%= link_to item, "#", class: "dropdown-item" %>
              <% end %>
            </div>
          </li>
        </ul>
      </div>

      <!-- User -->
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
          <% if user_signed_in? %>
            <li class='nav-item'>
              <%= current_user.email %>
            </li>
            <li class="nav-item dropdown">
            <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                <%= link_to "Add new tool", new_item_path, class: "dropdown-item" %>
                <%= link_to "Dashboard", dashboard_path, class: "dropdown-item" %>
                <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </div>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Login", new_user_session_path, class: "nav-link" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

<% else %>
  <nav class="navbar navbar-expand-lg navbar-light navbar-tool">

    <div class="left-menu-wrap">


      <!-- Logo -->
      <%= link_to root_path, class: "navbar-brand" do %>
        <%= image_tag "logo.svg", class:"logo" %>
        <% end %>

    <!-- Search form -->
    <form class="form-inline my-4 my-lg-0 search-navbar" >
<!--       <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="rent-button" type="submit">Search</button> -->
      <%= form_tag items_path, method: :get do %>
        <%= text_field_tag :query,
          params[:query],
          class: "form-control mr-sm-2 rounded-pill",
          placeholder: "Search..."
        %>
        <%= submit_tag "Search", class: "rent-button" %>
      <% end %>
    </form>
  </div>

    <div class="right-menu-wrap">

      <!-- Categories -->
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Categories
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <% Item::CATEGORIES.each do |item| %>
                <%= link_to item, "#", class: "dropdown-item" %>
              <% end %>
            </div>
          </li>
        </ul>
      </div>

      <!-- User -->
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
          <% if user_signed_in? %>
            <li class='nav-item'>
              <%= current_user.email %>
            </li>
            <li class="nav-item dropdown">
            <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                <%= link_to "Add new tool", new_item_path, class: "dropdown-item" %>
                <%= link_to "Dashboard", dashboard_path, class: "dropdown-item" %>
                <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </div>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Login", new_user_session_path, class: "nav-link" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
<% end %>
