
<%= simple_form_for [@item, @rental] do |f| %>
  <%= f.input :start_date, as: :date %>
  <%= f.input :end_date, as: :date %>
  <%= f.submit "Rent", class: "btn btn-primary btn-lg btn-block btn-booking" %>
<% end %>

<%= link_to "Back to Tool", @item  %>
